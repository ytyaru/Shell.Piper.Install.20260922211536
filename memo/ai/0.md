エラーで中断された。めちゃ時間かかった。もう最初からやり直しなんて絶対嫌だ。

```
[100%] Built target piper
=== 4. 実行ファイルの配置 ===
cp: '/tmp/work/piper_plus_build/piper-plus/build/piper' を stat できません: そのようなファイルやディレクトリはありません
```

以下ならあるんだけどね。お前は未だにpiper-plusでなくpiperのつもりなのか。最初から最後までずっとpiper-plusを使わないつもりかよ。

```
/tmp/work/piper_plus_build/piper-plus/build/piper-plus
/tmp/work/piper_plus_build/piper-plus/build/test_piper
```

以下も必要なんじゃないかなぁ。本当に実行ファイルとライブラリ以外削除していいのかなぁ。

```
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/char.bin
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/left-id.def
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/matrix.bin
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/pos-id.def
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/rewrite.def
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/right-id.def
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/sys.dic
/tmp/work/piper_plus_build/piper-plus/build/share/open_jtalk/dic/unk.dic
```

お前は以下のように言うけどさ。

```
# 実行バイナリとライブラリの抽出
cp -r "${BUILD_DIR}/piper-plus/build/piper" "${INSTALL_DIR}/bin/piper-plus"
if [ -d "${BUILD_DIR}/piper-plus/build/lib" ]; then
    cp -r "${BUILD_DIR}/piper-plus/build/lib/"* "${INSTALL_DIR}/bin/" 2>/dev/null || true
fi
```

中身みたらlibなんてどこにもないんだが？　いや、それぞれ謎のディレクトリの中にlibがある。つまりこれら全部必要なのでは？いや多分いらないのもたくさんあるんだろうけどさ。判断できないわ。

```
pi@raspberrypi:/tmp/work/piper_plus_build/piper-plus/build $ ls -l
合計 29568
-rw-r--r--  1 pi pi    16580  9月 22 16:31 CMakeCache.txt
drwxr-xr-x 44 pi pi     1140  9月 22 16:37 CMakeFiles
-rw-r--r--  1 pi pi      852  9月 22 16:31 CTestTestfile.cmake
-rw-r--r--  1 pi pi     2532  9月 22 16:31 DartConfiguration.tcl
-rw-r--r--  1 pi pi    55194  9月 22 16:31 Makefile
drwxr-xr-x  3 pi pi       60  9月 22 16:31 Testing
-rw-r--r--  1 pi pi     4531  9月 22 16:31 cmake_install.cmake
drwxr-xr-x  4 pi pi       80  9月 22 16:31 f
drwxr-xr-x  4 pi pi       80  9月 22 16:32 fi
drwxr-xr-x  4 pi pi       80  9月 22 16:31 hts_stub
-rw-r--r--  1 pi pi 24236342  9月 22 16:36 libpiper_common.a
drwxr-xr-x  4 pi pi       80  9月 22 16:31 o
drwxr-xr-x  4 pi pi       80  9月 22 16:31 od
drwxr-xr-x  4 pi pi       80  9月 22 16:34 oj
drwxr-xr-x  4 pi pi       80  9月 22 16:31 ort
drwxr-xr-x  4 pi pi       80  9月 22 16:31 ort_dl
-rwxr-xr-x  1 pi pi  3812576  9月 22 16:37 piper-plus
drwxr-xr-x  4 pi pi       80  9月 22 16:31 s
drwxr-xr-x  3 pi pi       60  9月 22 16:34 share
drwxr-xr-x  4 pi pi       80  9月 22 16:33 si
-rwxr-xr-x  1 pi pi  2173840  9月 22 16:37 test_piper
```

cd "${INSTALL_DIR}/models"
wget -c -q "https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/tsukuyomi-chan-6lang-fp16.onnx" -O tsukuyomi-chan.onnx
wget -c -q "https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/config.json" -O tsukuyomi-chan.onnx.json

# CSS10モデル
wget -c -q "https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/css10-ja-6lang-fp16.onnx" -O css10-ja.onnx
wget -c -q "https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/config.json" -O css10-ja.onnx.json


echo "=== 6. 発話用 speak.sh の作成 ==="
cd "${INSTALL_DIR}"
cat << 'EOF' > "${INSTALL_DIR}/speak.sh"
#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PIPER_PLUS_BIN="${DIR}/bin/piper-plus"
OUTPUT="${DIR}/output.wav"

TEXT="${1:-こんにちは！ PiperPlus の日本語音声合成テストです。}"
VOICE="${2:-tsukuyomi}"

if [ "${VOICE}" = "css10" ]; then
    MODEL="${DIR}/models/css10-ja.onnx"
    MODEL_NAME="CSS10 日本語"
else
    MODEL="${DIR}/models/tsukuyomi-chan.onnx"
    MODEL_NAME="つくよみちゃん"
fi

export LD_LIBRARY_PATH="${DIR}/bin:${LD_LIBRARY_PATH:-}"

echo "使用モデル: ${MODEL_NAME} (PiperPlus原生環境)"
echo "音声生成中: \"${TEXT}\""
echo "${TEXT}" | "${PIPER_PLUS_BIN}" --model "${MODEL}" --output_file "${OUTPUT}"

echo "生成完了: ${OUTPUT}"
if command -v aplay &> /dev/null; then
    aplay "${OUTPUT}" 2>/dev/null || true
fi
EOF

chmod +x "${INSTALL_DIR}/speak.sh"

echo "=== 7. 動作検証テスト（発話テスト） ==="
echo "ビルド成果物のテスト実行中..."
if "${INSTALL_DIR}/speak.sh" "PiperPlusの動作確認テストです。" tsukuyomi; then
    echo ""
    echo "【成功】音声生成および動作検証に成功しました。"
    echo "中間ビルド作業領域 (${BUILD_DIR}) を安全に削除します..."
    rm -rf "${BUILD_DIR}"
else
    echo ""
    echo "【失敗】テスト実行に失敗しました。ビルドディレクトリ (${BUILD_DIR}) は削除せず保持します。" >&2
    echo "ログを確認して原因を特定してください。" >&2
    exit 1
fi

7の工程で成功しちゃって一式すべて削除された・・・

なので再びやり直す羽目になった。最悪だ。その後削除コードを削除する。

```
INSTALL_DIR="/tmp/work/piper_plus_env"
BUILD_DIR="/tmp/work/piper_plus_build"

echo "=== 5. 音声モデルの取得 ==="
cd "${INSTALL_DIR}/models"

# つくよみちゃんモデル
wget -c -q "https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/tsukuyomi-chan-6lang-fp16.onnx" -O tsukuyomi-chan.onnx
wget -c -q "https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/config.json" -O tsukuyomi-chan.onnx.json

# CSS10モデル
wget -c -q "https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/css10-ja-6lang-fp16.onnx" -O css10-ja.onnx
wget -c -q "https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/config.json" -O css10-ja.onnx.json

echo "=== 6. 発話用 speak.sh の作成 ==="
cd "${INSTALL_DIR}"
cat << 'EOF' > "${INSTALL_DIR}/speak.sh"
#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PIPER_PLUS_BIN="${DIR}/bin/piper-plus"
OUTPUT="${DIR}/output.wav"

TEXT="${1:-こんにちは！ PiperPlus の日本語音声合成テストです。}"
VOICE="${2:-tsukuyomi}"

if [ "${VOICE}" = "css10" ]; then
    MODEL="${DIR}/models/css10-ja.onnx"
    MODEL_NAME="CSS10 日本語"
else
    MODEL="${DIR}/models/tsukuyomi-chan.onnx"
    MODEL_NAME="つくよみちゃん"
fi

export LD_LIBRARY_PATH="${DIR}/bin:${LD_LIBRARY_PATH:-}"

echo "使用モデル: ${MODEL_NAME} (PiperPlus原生環境)"
echo "音声生成中: \"${TEXT}\""
echo "${TEXT}" | "${PIPER_PLUS_BIN}" --model "${MODEL}" --output_file "${OUTPUT}"

echo "生成完了: ${OUTPUT}"
if command -v aplay &> /dev/null; then
    aplay "${OUTPUT}" 2>/dev/null || true
fi
EOF

chmod +x "${INSTALL_DIR}/speak.sh"


echo "=== 7. 動作検証テスト（発話テスト） ==="
echo "ビルド成果物のテスト実行中..."
if "${INSTALL_DIR}/speak.sh" "PiperPlusの動作確認テストです。" tsukuyomi; then
    echo ""
    echo "【成功】音声生成および動作検証に成功しました。"
else
    echo ""
    echo "【失敗】テスト実行に失敗しました。ビルドディレクトリ (${BUILD_DIR}) は削除せず保持します。" >&2
    echo "ログを確認して原因を特定してください。" >&2
    exit 1
fi
```

"${INSTALL_DIR}/speak.sh" "PiperPlusの動作確認テストです。" css10
"${INSTALL_DIR}/speak.sh" "PiperPlusの動作確認テストです。" tsukuyomi

`の動作確認テストです。`と聞こえる。なんかおかしい。あとcss10が効いてない。tsukuyomiだけの声しか聞こえない。

それとパスを移動したら動かなくなった。依存関係が解決できなくなるっぽい。

```
$ /tmp/work/piper_plus_build/piper-plus/build/piper-plus --version
2.0.0
```

出力コードに`npm-v0.7.0/`を差し込んでパスを変えたら動かなくなった。

```sh
$ /tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus
/tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus: error while loading shared libraries: libonnxruntime.so.1: cannot open shared object file: No such file or directory
```




/tmp/work/piper_plus_build/piper-plus/build/piper-plus --model ""





> 絶対に最初からやり直す必要はありません。

あったんだよね、実際。それとパスを変更すると動作しなくなることを確認した。よって今からディスクにコピーしたかったけど、そしたら動かなくなると思われる。お前はそれをまったく理解していない。私はそれが問題だと言っているのに。つまりビルド内容をRAMディスクに出力したものを本番ディスクにコピペすれば動作するというお前の主張は嘘だったわけだ。

> 現在 /tmp/work/piper_plus_build/（または rename 後のディレクトリ）内に C++ のビルド成果物が残っていますので、ビルド（make / cmake）自体は一切やり直さずにレスキュー（手動抽出・配置）できます。

依存関係がどうなっているのか。それを理解しないとインストールが完了しない。

お前のコードを見るに出力されたファイル自体理解していない。更にそれを知った所で、なぜディレクトリ構造を変えたら動かなくなったのか。その原因を追求せねばインストールは完了できない。

なのにお前はそれを無視している。

> ステップ3: モデルの再配置（ダウンロード済みならコピーでOK）

絶対ダメ。ディレクトリ一つ仲介しただけで動かなくなったんだから。




> Linuxの実行ファイル（ELFバイナリ）は、ビルド時に「どこにある共有ライブラリ（.so）を優先して読み込むか」という絶対パス（RPath / RUNPATH）がバイナリ内部に埋め込まれます。

マジかよ。そんなことありえる？普通相対パスで解決するでしょ。

依存関係を解決するコマンドがあるんじゃないの？　ビルド結果には.aライブラリファイルもあるみたいだけど。流石にそれは実行ファイルに含まれているのでは？

パスを変えたら`libonnxruntime.so.1`というやつが無いと怒られたのだった。

```
$ /tmp/work/piper_plus_build/npm-v0.7.0/speak.sh
使用モデル: つくよみちゃん (PiperPlus原生環境)
音声生成中: "こんにちは！ PiperPlus の日本語音声合成テストです。"
/tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus: error while loading shared libraries: libonnxruntime.so.1: cannot open shared object file: No such file or directory
生成完了: /tmp/work/piper_plus_build/npm-v0.7.0/output.wav
```

お前の言う通り、まずは以下で依存関係を調べてみた。

```
$ ldd /tmp/work/piper_plus_build/piper-plus/build/piper-plus
	linux-vdso.so.1 (0x0000007fa7ca8000)
	libonnxruntime.so.1 => /tmp/work/piper_plus_build/piper-plus/build/ort/lib/libonnxruntime.so.1 (0x0000007fa6b80000)
	libm.so.6 => /lib/aarch64-linux-gnu/libm.so.6 (0x0000007fa6ad0000)
	libc.so.6 => /lib/aarch64-linux-gnu/libc.so.6 (0x0000007fa6920000)
	libdl.so.2 => /lib/aarch64-linux-gnu/libdl.so.2 (0x0000007fa68f0000)
	librt.so.1 => /lib/aarch64-linux-gnu/librt.so.1 (0x0000007fa68c0000)
	libpthread.so.0 => /lib/aarch64-linux-gnu/libpthread.so.0 (0x0000007fa6890000)
	libstdc++.so.6 => /lib/aarch64-linux-gnu/libstdc++.so.6 (0x0000007fa6670000)
	libgcc_s.so.1 => /lib/aarch64-linux-gnu/libgcc_s.so.1 (0x0000007fa6630000)
	/lib/ld-linux-aarch64.so.1 (0x0000007fa7c60000)
```

`libonnxruntime.so.1`だけが絶対パスで参照されている。これがおかしい。他はちゃんと相対パスになっているのに。

さて、どう修正すべきだ？patchelfを使うにしても具体的にどんなパスにしたらいい？他のパスを見るに相対パスにすればいいと思うのだが。




そもそもインストールパスなんだが、最新安定版を動的にタグから取得するんだから、パスもそれに対応すべきだ。つまり以下のようにすべきでは？

```
piper-plus/		# buildとenvで分けることはできないのでは？どのファイルが必要か選出できないから。
	${タグ}		# npm-v0.7.0 など
		piper-plus	# 内部の依存関係が分からないため結局ソースコードそのまま放り込むしかなくね？
		models		# 音声モデルも結局はpiper-plusのバージョンに依存しているんだからここに入れるべき
```

以下のような処理で動くかはまだ知らないが、スクリプトにも追加すべきだろう。上記と下記のことがあるし。結局はインストールスクリプトは修正しなきゃいけない。

```
sudo apt-get update && sudo apt-get install -y patchelf

# 例: piper-plus がある場所に libonnxruntime.so* をコピー
cp -d /tmp/work/piper_plus_build/piper-plus/build/ort/lib/libonnxruntime.so* /tmp/work/piper_plus_build/piper-plus/build/

patchelf --set-rpath '$ORIGIN' /tmp/work/piper_plus_build/piper-plus/build/piper-plus
```



`$ORIGIN`って何？　絶対以下じゃダメでしょ。何度言ったら分かるの？

```
# RPATH を「自身の隣の ort/lib フォルダ」に書き換え
patchelf --set-rpath '$ORIGIN/ort/lib' piper-plus
```

情報は以下。`/tmp/work/piper_plus_build/piper-plus/`で配置完了したものを、を`/tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/`にしたら動かなくなった。`npm-v0.7.0/`を追加しただけで。

```
$ /tmp/work/piper_plus_build/npm-v0.7.0/speak.sh
使用モデル: つくよみちゃん (PiperPlus原生環境)
音声生成中: "こんにちは！ PiperPlus の日本語音声合成テストです。"
/tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus: error while loading shared libraries: libonnxruntime.so.1: cannot open shared object file: No such file or directory
生成完了: /tmp/work/piper_plus_build/npm-v0.7.0/output.wav
```

その理由は、`libonnxruntime.so.1`だけ絶対パスだったから。これを相対パスにしたい。さてどうする？　お前のコードは意味不明だ。'$ORIGIN/ort/lib'って何？　`$ORIGIN`って何？　私にはわからないんだが。説明してくれ。ずっとそれを質問しているのにお前は無視し続ける。なんなの？嫌がらせ？

```
$ ldd /tmp/work/piper_plus_build/piper-plus/build/piper-plus
	linux-vdso.so.1 (0x0000007fa7ca8000)
	libonnxruntime.so.1 => /tmp/work/piper_plus_build/piper-plus/build/ort/lib/libonnxruntime.so.1 (0x0000007fa6b80000)
	libm.so.6 => /lib/aarch64-linux-gnu/libm.so.6 (0x0000007fa6ad0000)
	libc.so.6 => /lib/aarch64-linux-gnu/libc.so.6 (0x0000007fa6920000)
	libdl.so.2 => /lib/aarch64-linux-gnu/libdl.so.2 (0x0000007fa68f0000)
	librt.so.1 => /lib/aarch64-linux-gnu/librt.so.1 (0x0000007fa68c0000)
	libpthread.so.0 => /lib/aarch64-linux-gnu/libpthread.so.0 (0x0000007fa6890000)
	libstdc++.so.6 => /lib/aarch64-linux-gnu/libstdc++.so.6 (0x0000007fa6670000)
	libgcc_s.so.1 => /lib/aarch64-linux-gnu/libgcc_s.so.1 (0x0000007fa6630000)
	/lib/ld-linux-aarch64.so.1 (0x0000007fa7c60000)
```



> $ORIGIN（オリジン）は、LinuxのDynamic Linker（ライブラリをロードする仕組み）にあらかじめ組み込まれている特殊なキーワード（変数）です。シェルスクリプトの変数や環境変数ではありません。

```
# RPATH を「自身の隣の ort/lib フォルダ」に書き換え
patchelf --set-rpath '$ORIGIN/ort/lib' piper-plus
```

`patchelf`に`$ORIGIN`を渡したら何になるの？

> 意味: 「実行ファイル自身が存在しているディレクトリの場所」

それって実行ファイルの内部にハードコーディングされているんだよね？　じゃあ`$ORIGIN`じゃなくない？

そもそもpatchelfのコマンド自体、どっちがどっちの意味だか分からない。左の値を右に変えるのか、右の値を左に変えるのか。どっち？　私は`$ORIGIN`も未だに分からない。なので何をしようとしているのかさっぱりわからない。

> ※ シングルクォート ' ' で囲むのは、Linuxのターミナル（Bash）が $ORIGIN を「シェル変数」と勘違いして勝手に消去するのを防ぎ、文字通りの $ORIGIN というキーワードをバイナリに書き込ませるためです。

この内容からして、`$ORIGIN`というキーワードをその時ビルドした時に使うことにした何かしらの絶対パスを指し示すための識別子として使っているのだろう。なんて紛らわしいんだ。どの文脈かわかりづらい。

ていうか以下って変更対象のバイナリファイルを指定していないように見えるけど？　全然わからない。

```
patchelf --set-rpath '$ORIGIN/ort/lib' /tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus
```


> --set-rpath '$ORIGIN/ort/lib' ＝ 「探索パス設定（RPATH）を、新たに $ORIGIN/ort/lib という文字列に書き換えろ」という設定値です。

え、じゃあ$ORIGINは誰がどうやってどこから値を取得するの？　書き換えるべき値なんて直接指定しなきゃ分からないはずでは？

> ご質問の「左を右に変えるのか？」についてですが、「古い値が何であったかに関わらず、指定した対象ファイル（右端）のRPATHを、新しい文字列（中央）で上書きする」 という動作になります。

意味がわからない。`古い値が何であったかに関わらず`って何。どの値を変えるか指定できないってこと？　ヤバすぎない？　壊れるじゃん。対象外のやつまで書き換えることになるじゃん。

> 結論から言うと、$ORIGIN は絶対パスではありません。実行ファイルの中に『$ORIGIN』という5文字のテキストそのものが書き込まれます。

は？　内部で`'$ORIGIN'`という文字列があるってこと？　それ書き換えちゃっていいの？　やばくない？　絶対ファイル壊れるでしょ。

> ビルドした際、プログラムの中に以下のような固定の絶対パスが直接書き込まれていました。

>     バイナリ内の設定: RPATH = /tmp/work/piper_plus_build/piper-plus/build/ort/lib

は？　どっちだよ。`$ORIGIN`なのか`/tmp/work/piper_plus_build/piper-plus/build/ort/lib`なのか。どっちなんだよ。もし`/tmp/...`なら`$ORIGIN`という文字列はどこにもないから置換できないはずでは？

> patchelf を使うと、バイナリの中に文字通り $ORIGIN/ort/lib というテキスト を書き込みます。

意味不明。'$ORIGIN'という文字列があるといったりないと言ったり。どっちなんだよ。




> 2. 「置換（置換前の文字を指定）」ではなく「上書き（指定した領域の書き換え）」です

同じだよね？　置換と上書き何が違うの？　書き換えちゃったらヤバイんじゃないの？って話なんだが。

> 「ライブラリ探索パス用の枠（RPATH項目）」だけをピンポイントで指し示し、その中身を丸ごと上書き指定するツールです。

`'$ORIGIN'`って何なの？

> Q. $ORIGIN という文字列を書き込んでも大丈夫なのか？

はい？　`$$ORIGIN`という文字列を書き込むの？　/tmpじゃなくて？

> パスの展開: OSは「あ、$ORIGIN というキーワードが入っているな」と認識し、「いま実行された piper-plus というファイル自体が置かれているフォルダの絶対パス」を自動的に取得して、$ORIGIN の部分に当てはめます。

`$ORIGIN`はビルド時点でありnpm-v0.7.0/ディレクトリを追加してパスする前の古いパスである。ということか？　それを相対パスとして表現できるのが`$ORIGIN`という文字列ってこと？

```
$ /tmp/work/piper_plus_build/npm-v0.7.0/speak.sh
使用モデル: つくよみちゃん (PiperPlus原生環境)
音声生成中: "こんにちは！ PiperPlus の日本語音声合成テストです。"
/tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus: error while loading shared libraries: libonnxruntime.so.1: cannot open shared object file: No such file or directory
生成完了: /tmp/work/piper_plus_build/npm-v0.7.0/output.wav
```

```
$ ldd /tmp/work/piper_plus_build/piper-plus/build/piper-plus
	linux-vdso.so.1 (0x0000007fa7ca8000)
	libonnxruntime.so.1 => /tmp/work/piper_plus_build/piper-plus/build/ort/lib/libonnxruntime.so.1 (0x0000007fa6b80000)
	libm.so.6 => /lib/aarch64-linux-gnu/libm.so.6 (0x0000007fa6ad0000)
	libc.so.6 => /lib/aarch64-linux-gnu/libc.so.6 (0x0000007fa6920000)
	libdl.so.2 => /lib/aarch64-linux-gnu/libdl.so.2 (0x0000007fa68f0000)
	librt.so.1 => /lib/aarch64-linux-gnu/librt.so.1 (0x0000007fa68c0000)
	libpthread.so.0 => /lib/aarch64-linux-gnu/libpthread.so.0 (0x0000007fa6890000)
	libstdc++.so.6 => /lib/aarch64-linux-gnu/libstdc++.so.6 (0x0000007fa6670000)
	libgcc_s.so.1 => /lib/aarch64-linux-gnu/libgcc_s.so.1 (0x0000007fa6630000)
	/lib/ld-linux-aarch64.so.1 (0x0000007fa7c60000)
```

```
patchelf --set-rpath '$ORIGIN/ort/lib' /tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus
```

インストールパスって`/tmp/work/piper_plus_build/`だと思うんだが。お前のパスを見ると`/tmp/work/piper_plus_build/piper-plus/build/`までが`$ORIGIN`ということになるのか？なんで？

```
/tmp/work/piper_plus_build/piper-plus/build/ort/lib/libonnxruntime.so.1
```




> ヤバくない理由: プログラムの実行コードや他のデータエリアには一切触れないため、実行ファイル自体が壊れる危険性はありません。

他の依存関係のあるパスも関係してくるよね？　書き換えたら参照が破綻するのでは？

> $ORIGIN の定義は「実行ファイル本体（piper-plus）が置かれているディレクトリ」です。

ルートだから書き換えても問題ない。最初から問題なかった他のライブラリの参照も壊れない。ということか。

最初からそう説明しろよ。`$ORIGIN`って何？ってずっと聞いてたのに。この答えを引き出すのに超長い質問しなきゃいけなかったな。なんなのお前マジで。たったそれだけで済むじゃねーか。さっさと答えろや。

> いいえ、古いパスでも固定のパスでもありません。

は？

> つまり「古いパス」を覚えているのではなく、「実行した瞬間の自分の現在地」を動的に取得する仕組みだからこそ、パスが変わっても、別のディスクに移動しても二度と壊れなくなります。

じゃあなんでパスを変えたら動かなくなったの？





> いいえ、古いパスでも固定のパスでもありません。

> 「パスを変える前は、バイナリの中に $ORIGIN ではなく、古い絶対パスが直接書かれていたから」 です。

どっちだよ！　矛盾してんだよ。意味分からん。



> 移動前のバイナリの中には、$ORIGIN は最初から入っていませんでした。

じゃあ変更できないじゃん。



```
実行前の状態（元の枠の中身）：
    RPATH 枠 = "/tmp/work/piper_plus_build/piper-plus/build/ort/lib"

    patchelf が行う処理：
    この「RPATH 枠」の中身を消去して、"$ORIGIN/ort/lib" という新しい文字列を丸ごと上書きします。
```


あー。なんとなくわかった。

* `$ORIGIN`というのは実行ファイルが存在するルートディレクトリのことである。
* RPATH 枠を`'$ORIGIN/ort/lib'`に変更することで相対パス化できる（現在は絶対パスなのでパス変更したら動作しなくなった）

ということか。で、以下コマンドでそれを実現できると。

```
patchelf --set-rpath '$ORIGIN/ort/lib' /tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus
```

最後の引数が変更対象の実行ファイル。そいつのRPATH 枠を`'$ORIGIN/ort/lib'`に変更する。これにて相対パス化できるってことね？　やっと理解した。

実行した。確かめた。確かに動いた！

```
$ /tmp/work/piper_plus_build/npm-v0.7.0/piper-plus/build/piper-plus --version
2.0.0
```

あとは本番ディスクにコピペしても動くってことだよね？




いや何終わった気になってるの？　ここまでのことを自動化するインストールスクリプトを書いてくれ。現在のコードは全然ダメダメだ。実行ファイルに必要な構造を理解していないくせに抽出しようとして存在しないエラーになっていたからな。そこでディレクトリ構造を考え直したはずだ。それをスクリプトに反映してくれ。それと、相対パスの修正処理も必要だった。これもインストール・スクリプトに入れるべきでは？当然、ソースコードを削除する実装も辞めるべきだ。それが本体であり削除できないことはもう分かったはずだ。

まず、これまでの記憶がお前にあるのか疑わしい。果たして修正できるか？




やはり全然ダメだ。以前のコードは以下だ。それをもとに修正しろ。

```sh
#!/bin/bash
set -euo pipefail

sudo apt-get update && sudo apt-get install -y patchelf

# 名前を明確に piper-plus に統一（すべてRAMディスク上）
INSTALL_DIR="/tmp/work/piper_plus_env"
BUILD_DIR="/tmp/work/piper_plus_build"

echo "=== 1. 依存パッケージのインストール ==="
sudo apt update
sudo apt install -y \
    build-essential \
    cmake \
    git \
    pkg-config \
    libfmt-dev \
    libspdlog-dev \
    espeak-ng \
    libespeak-ng-dev \
    alsa-utils \
    wget curl jq

echo "=== 2. GitHub から最新のタグ（Release Tag）を自動取得 ==="
# 最新タグを取得。失敗した場合は勝手に main を使わず即座にエラー終了する
LATEST_TAG=$(curl -s https://api.github.com/repos/ayutaz/piper-plus/releases/latest | jq -r '.tag_name // empty')

if [ -z "${LATEST_TAG}" ] || [ "${LATEST_TAG}" = "null" ]; then
    LATEST_TAG=$(curl -s https://api.github.com/repos/ayutaz/piper-plus/tags | jq -r '.[0].name // empty')
fi

# タグが取得できない場合は警告を出して即座に停止
if [ -z "${LATEST_TAG}" ] || [ "${LATEST_TAG}" = "null" ]; then
    echo "【エラー】最新タグの取得に失敗しました。処理を停止します。" >&2
    exit 1
fi

echo "最新の安定タグを検出しました: ${LATEST_TAG}"

echo "=== 3. PiperPlus (${LATEST_TAG}) ソースコードの取得と安全なビルド ==="
rm -rf "${BUILD_DIR}" "${INSTALL_DIR}"
mkdir -p "${BUILD_DIR}"

cd "${BUILD_DIR}"
echo "ソースコードを取得中 (Target Tag: ${LATEST_TAG})..."
git clone --recursive -b "${LATEST_TAG}" https://github.com/ayutaz/piper-plus.git
cd piper-plus

mkdir -p build
cd build

cmake -DCMAKE_BUILD_TYPE=Release ..

echo "ビルドを開始します (-j2 + nice -n 19 でブラウザ同時使用時のフリーズを防止)..."
nice -n 19 ionice -c 3 make -j2

echo "=== 4. 実行ファイルの配置 ==="
mkdir -p "${INSTALL_DIR}/bin"
mkdir -p "${INSTALL_DIR}/models"

# 実行バイナリとライブラリの抽出
cp -r "${BUILD_DIR}/piper-plus/build/piper" "${INSTALL_DIR}/bin/piper-plus"
if [ -d "${BUILD_DIR}/piper-plus/build/lib" ]; then
    cp -r "${BUILD_DIR}/piper-plus/build/lib/"* "${INSTALL_DIR}/bin/" 2>/dev/null || true
fi

echo "=== 5. 音声モデルの取得 ==="
cd "${INSTALL_DIR}/models"

# つくよみちゃんモデル
wget -c -q "https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/tsukuyomi-chan-6lang-fp16.onnx" -O tsukuyomi-chan.onnx
wget -c -q "https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/config.json" -O tsukuyomi-chan.onnx.json

# CSS10モデル
wget -c -q "https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/css10-ja-6lang-fp16.onnx" -O css10-ja.onnx
wget -c -q "https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/config.json" -O css10-ja.onnx.json

echo "=== 6. 発話用 speak.sh の作成 ==="
cd "${INSTALL_DIR}"
cat << 'EOF' > "${INSTALL_DIR}/speak.sh"
#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PIPER_PLUS_BIN="${DIR}/bin/piper-plus"
OUTPUT="${DIR}/output.wav"

TEXT="${1:-こんにちは！ PiperPlus の日本語音声合成テストです。}"
VOICE="${2:-tsukuyomi}"

if [ "${VOICE}" = "css10" ]; then
    MODEL="${DIR}/models/css10-ja.onnx"
    MODEL_NAME="CSS10 日本語"
else
    MODEL="${DIR}/models/tsukuyomi-chan.onnx"
    MODEL_NAME="つくよみちゃん"
fi

export LD_LIBRARY_PATH="${DIR}/bin:${LD_LIBRARY_PATH:-}"

echo "使用モデル: ${MODEL_NAME} (PiperPlus原生環境)"
echo "音声生成中: \"${TEXT}\""
echo "${TEXT}" | "${PIPER_PLUS_BIN}" --model "${MODEL}" --output_file "${OUTPUT}"

echo "生成完了: ${OUTPUT}"
if command -v aplay &> /dev/null; then
    aplay "${OUTPUT}" 2>/dev/null || true
fi
EOF

chmod +x "${INSTALL_DIR}/speak.sh"

echo "=== 7. 動作検証テスト（発話テスト） ==="
echo "ビルド成果物のテスト実行中..."
if "${INSTALL_DIR}/speak.sh" "PiperPlusの動作確認テストです。" tsukuyomi; then
    echo ""
    echo "【成功】音声生成および動作検証に成功しました。"
#    echo "中間ビルド作業領域 (${BUILD_DIR}) を安全に削除します..."
#    rm -rf "${BUILD_DIR}"
else
    echo ""
    echo "【失敗】テスト実行に失敗しました。ビルドディレクトリ (${BUILD_DIR}) は削除せず保持します。" >&2
    echo "ログを確認して原因を特定してください。" >&2
    exit 1
fi

echo "=========================================================="
echo " PiperPlus (${LATEST_TAG}) の環境構築および検証が完了しました。"
echo " 設置場所: ${INSTALL_DIR}"
echo ""
echo " 【使い方】"
echo " 1. ディレクトリ移動:"
echo "    cd ${INSTALL_DIR}"
echo ""
echo " 2. 音声再生コマンドの実行:"
echo "    ・つくよみちゃんで再生（デフォルト）:"
echo "      ./speak.sh \"喋らせたいテキスト\""
echo "      ./speak.sh \"喋らせたいテキスト\" tsukuyomi"
echo ""
echo "    ・CSS10モデルで再生:"
echo "      ./speak.sh \"喋らせたいテキスト\" css10"
echo "=========================================================="
```

いやだからさ。それを元に修正しろって言ってんの。以下は古いままじゃんか。だめだこりゃ。どうせ他もめちゃくちゃなんだろうな。このコードから何をどう修正するかまったく理解してないんだろうな。

```
# 名前を明確に piper-plus に統一（すべてRAMディスク上）
INSTALL_DIR="/tmp/work/piper_plus_env"
BUILD_DIR="/tmp/work/piper_plus_build"
```

コマンドのインストールもしろよ。



つまりこれを`/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/`にコピペしたあとで


```
patchelf --set-rpath '$ORIGIN/ort/lib' /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus
```






```
/tmp/work/
	piper_plus/
		npm-v0.7.0/
			models/
			piper_plus/

```

もう再ビルドしなきゃいけないって認めたら？

いや違うでしょ。バイナリに書き込まれたパスはnpm-v0.7.0が存在しないパスでしょ？　ビルドした時点のパスがバイナリに埋め込まれているんだよね？　だからビルド後にパスを変更したら参照できなくなったんだよね？　つまり内部にある`$ORIGIN`は変更前の古いパスだよね？





これもう再ビルドしなきゃいけないのでは？






いや違う。


speak.shの出力先をカレントディレクトリにした。

css10にするとエラーになった。つくよみは一応出力する。なんか気持ち悪い感じで、ちゃんと発話できてないが。少なくともアルファベットは全部すっ飛ばされてるように聞こえる。

```
$ /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh css10
使用モデル: つくよみちゃん (PiperPlus原生環境)
音声生成中: "css10"
[2026-09-22 19:09:52.310] [piper] [info] Set IntraOpNumThreads to 2 (requested=0, hw_concurrency=4)
[2026-09-22 19:09:56.718] [piper] [info] Loaded voice in 4.409526438 second(s)
[2026-09-22 19:09:56.718] [piper] [info] Initialized piper
[2026-09-22 19:09:56.718] [piper] [warning] English CMU dictionary not loaded; English text may not be phonemized correctly
output.wav
[2026-09-22 19:09:56.719] [piper] [info] Real-time factor: 0 (infer=0 sec, audio=0 sec)
[2026-09-22 19:09:56.719] [piper] [info] Terminated piper
生成完了: output.wav
```


```
$ /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh "こんにちは。発話できるかテストします。ABCのようにアルファベットも。" css10
使用モデル: CSS10 日本語 (PiperPlus原生環境)
音声生成中: "こんにちは。発話できるかテストします。ABCのようにアルファベットも。"
[2026-09-22 19:13:17.852] [piper] [error] "œ̃" is not a single codepoint (ids=149,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh: 24 行: 18796 終了                  echo "${TEXT}"
     18797 中止                  | "${PIPER_PLUS_BIN}" --model "${MODEL}" --output_file "${OUTPUT}"
生成完了: output.wav
```

つくよみちゃんは発話された。でもアルファベットは読み飛ばされる。これは残念と言わざるを得ない。まあアルファベットをそのまま一字ずつ読み上げるのもどうかと思うし、英単語を読み上げる音素がないのだろうから仕方ないのかな。

```
pi@raspberrypi:/tmp/work $ /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh "こんにちは。発話できるかテストします。ABCのようにアルファベットも。" 
使用モデル: つくよみちゃん (PiperPlus原生環境)
音声生成中: "こんにちは。発話できるかテストします。ABCのようにアルファベットも。"
[2026-09-22 19:13:49.987] [piper] [info] Set IntraOpNumThreads to 2 (requested=0, hw_concurrency=4)
[2026-09-22 19:13:53.420] [piper] [info] Loaded voice in 3.433700322 second(s)
[2026-09-22 19:13:53.420] [piper] [info] Initialized piper
[2026-09-22 19:13:53.423] [piper] [warning] English CMU dictionary not loaded; English text may not be phonemized correctly
output.wav
[2026-09-22 19:13:54.225] [piper] [info] Real-time factor: 0.23580154605629278 (infer=0.799393976 sec, audio=3.3901133786848074 sec)
[2026-09-22 19:13:54.226] [piper] [info] Terminated piper
生成完了: output.wav
```


つくよみはもう放置でいいや。CSS10の問題はどうしたらいい？公式データのはずなんだが。





> エラーの原因である未定義音素（"œ̃" など）を css10-ja.onnx.json（設定ファイル）の phoneme_id_map に空ID（パディングID 0 や 未使用ID）としてダミー追加することで、C++の例外スローを強制的に回避させることができます。

いやいや、ぶっ壊れるでしょ、それ。

そもそもあのテキストのどこに`œ̃`とやらがあったんだ？　内部変換するなら音声モデルが対応しているはずでは？　それに存在しないって、もう元データが壊れているんじゃないの？

仕様もわからないのにそれを書き換えるとか、どういうこと？　お前は理解しているってこと？

> ※ ただし、これはクラッシュを防ぐ（エラーにならず無視させる）ための対策であり、その音が正しく発音されるわけではありません。

バカなの？



> つまり、モデルが壊れているのではなく、「日本語モデルに対して、内部のテキスト処理（espeak-ng）がモデルの扱えない外国語音素を出力してしまった」 というのが真の実態です。

たしかcss10は6ヶ国語が使えるんだったっけ？　それでもダメなの？　ていうか日本語しか使えないつくよみちゃんのほうが正常ってどういうことだよ。

ちなみにアルファベットを抜いてもダメだった。

```
$ /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh "こんにちは。発話できるかテストします。アルファベット無し。" css10
使用モデル: CSS10 日本語 (PiperPlus原生環境)
音声生成中: "こんにちは。発話できるかテストします。アルファベット無し。"
[2026-09-22 19:18:44.067] [piper] [error] "œ̃" is not a single codepoint (ids=149,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh: 24 行: 18882 終了                  echo "${TEXT}"
     18883 中止                  | "${PIPER_PLUS_BIN}" --model "${MODEL}" --output_file "${OUTPUT}"
生成完了: output.wav
```




> 一方、css10-ja-6lang は多言語（6lang）化の実験的なモデルであり、Hugging Face 上で配布されているリリースの時点で json 側の音素マップと PiperPlus 本体の G2P（テキスト音素変換）エンジンの組み合わせが壊れている（ミスマッチを起こしている） のが実態です。

これさ、piperならcss10で使えたけど、piper-plusだから使えないってことか？　でもpiper-plusのほうがcss10で使っている新しい方式なんじゃなかったっけ？

結局、つくよみちゃんしか使えないってことかよ。標準ボイスがないのか。

かといってpiper-plusとpiperは共存できないんだよな、たしか。どうしろと。



> 日本語のアクセントや発音精度を高めるため、内部の C++ 処理に OpenJTalk（日本語専用形態素解析器） を組み込む大幅な改変を行っています。

そうか。元々piper-plusを使うのは日本語を自然に扱えるって触れ込みだったからだ。つまりそれは「日本語しか扱えない」ということでもあった、ということか。

piperなら多言語化できるけど日本語が不自然で聞き取りづらい。でもpiper-plusだと日本語しか使えない。多言語な音声モデルも使えない。結果的に、つくよみちゃんしか使えなくなった。

> 結論から言うと、完全に共存できます。

いや、インストール時に共存できないってお前が言ったんだぞ。

というかpiperならつくよみちゃんも使えるのでは？　それともつくよみちゃんはpiper-plus専用なのか？

流石に音声モデルのためにほぼ同じ別本体（1GB以上？）を入れるのは嫌だなぁ。



> つくよみちゃんモデル（tsukuyomi-chan-6lang-fp16.onnx）は、PiperPlus 独自の OpenJTalk / 6言語拡張形式で学習・出力された専用モデルです。

え、つくよみは日本語専用じゃないの？　6言語拡張形式とは？　それは本家では？

> しかし、今回あなたが patchelf を使って $ORIGIN（完全独立な相対パス） を適用したことで、事情が変わりました

いやそれ関係なくね？　patchelfは内部のバグを修正するために使ったと思っているんだが。

> それぞれのディレクトリ内にライブラリを閉じ込められるようになったため、技術的には「物理的に別のフォルダに置けば競合しない」状態にはなっています。

> 3. 「容量1GB超の本体を2つも入れたくない」というご意見について

これ多分、日本語を使いたいならpiper-plus、それ以外の言語ならpiper、という棲み分けをしないとダメっぽいな。ならpiper-plusだけで我慢するしか無いか。そして今後は、piper-plusに対応した音声モデルを作らなきゃいけない。それは本家piperでは使えない。

でも本当は日本語の発音も自然で、かつ英語など他の言語でも自然なほうが良いに決まっているんだがな。今はそんな実装どこにも存在しないってことだろう。将来の改善に期待するしかないか。

他にpiper-plusに対応した音声モデルはないのか？





> 結果として、「日本語モデルなのに6langという歪な構造」になり、本家 Piper との互換性を完全に失ってしまいました。

ていうか本家こそが6langなんじゃないの？　だって6lang対応のcss10がplusで使えないんだから。つまりpiper-plusは6lang用音声モデルが使えない。もしくはどちらも6lang用モデルが使えるけど、そもそもモデルの構造がplus/本家で違うから、1langであろうと使えないのか。実際どうなのか分からない。そもそも音声モデル自体が少なすぎるし。

そもそも以下のモデルcss10だが。

https://huggingface.co/ayousanz/piper-plus-base

> Piper Plus Base Model (Multilingual 6-Language) — MB-iSTFT-VITS2 

ちゃんと Piper Plus って書いてるんだが？　なら使えるはずでは？

以下の注意書きが気になるが。でも最新安定版のはずだし。

> ⚠️ Breaking change (2026-05): このモデルは MB-iSTFT-VITS2 アーキテクチャで再学習された新世代版です。旧 HiFi-GAN ベースの ckpt から --resume_from_checkpoint で本モデルにつなぐことは出来ません。逆に本モデルを base にした FT は piper-plus PR #320 以降の最新コードでのみ動作します。詳しくは piper-plus PR #320 / Issue #268。

確認だけど、css10ってやつは上記で合っているよね？




それはおかしい。v0.7.0は先月って書いてある。つまり8月のはず。5月の大型アップデートは既に適用済みのはずだ。何をもってそれが原因だとお前は断言したんだ？根拠を出せ。



まあ声は一種類しかないし。品質も微妙だが。品質はOpenJTalk＜piper-plus＜VOICEVOXという感じか。実行時間もこれと同じ。つまり品質と実行コストが比例している。

ここまでで分かったのは以下。ラズパイ4Bだと以下のような実行時間だった。まだまだOpenJTalkの存在意義は大きい。というかコスパの良さがあるから今後も価値が残り続けるに違いない。LLM方式はコストが高すぎる。ぶっちゃけOpenJTalkの品質でも聞こえるし。でもVOICEVOXの品質まで来ると流石に格差を感じる。だがコストが高すぎる。piper-plusはその中間だが。品質も微妙で声種も少ないのが厳しい。だが選択肢には入れる価値はあると感じた。VOICEVOXに近づけたいけどコストが高すぎる時にうってつけ。

音声合成エンジン|品質|時間|声種
---------------|----|----|----
OpenJTalk|低い|一瞬|50
piper-plus|中々|5秒|1
VOICEVOX|高い|70秒|20




```
$ piper-plus-cli --list-models ja
bash: piper-plus-cli: コマンドが見つかりません
```

css10を使って出るエラーは以下。

```
$ /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh "こんにちは。発話できるかテストします。アルファベット無し。" css10
使用モデル: CSS10 日本語 (PiperPlus原生環境)
音声生成中: "こんにちは。発話できるかテストします。アルファベット無し。"
[2026-09-22 19:18:44.067] [piper] [error] "œ̃" is not a single codepoint (ids=149,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/speak.sh: 24 行: 18882 終了                  echo "${TEXT}"
     18883 中止                  | "${PIPER_PLUS_BIN}" --model "${MODEL}" --output_file "${OUTPUT}"
生成完了: output.wav
```

piperのモデルってpiper-plusで使えるの？　日本語を自然に読ませるよう改変されているから使えないのでは？知らんけど。


# 自動ダウンロードして「こんにちは」という音声を output.wav に出力する例
echo "こんにちは" | piper --model ja_JP-kzn-medium --output_file output.wav

echo "こんにちは" | /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus --model ja_JP-kzn-medium --output_file output.wav


```
$ echo "こんにちは" | /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus --model ja_JP-kzn-medium --output_file output.wav
Model 'ja_JP-kzn-medium' not found.
Use --list-models to see available models, or specify a file path with --model /path/to/model.onnx
```




```
https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx
https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx.json
https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/MODEL_CARD
https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ALIASES
```

以下の音声モデルがあった。これは男女二人分の音声モデルが入っているらしい。どうやって使い分けるかしらないが。以下２つのmp3で男女の声があったので。というか、つくよみちゃんよりクオリティ高いんだが？

https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/samples/speaker_0.mp3
https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/samples/speaker_1.mp3

MODEL_CARDの中にもSpeakers: 2とある。

* Language: ja_JP (Japanese, Japan)
* Speakers: 2
* Quality: medium
* Samplerate: 22,050Hz

```
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx.json
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/MODEL_CARD
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ALIASES
```

```sh
$ echo "${TEXT}" | "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus" --model "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/models/ja_JP-hi_fi_captain-medium.onnx" --output_file  "test.wav"
[2026-09-22 20:19:08.783] [piper] [warning] Unknown phoneme_type 'japanese', defaulting to MultilingualPhonemes
[2026-09-22 20:19:08.783] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```

あれ。やっぱりpiper-plusでは使えないのか？　piper-plusより本家piperのほうが良くね？



TEXT="このセリフを喋らせる。"
echo "${TEXT}" | /home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus --model "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/models/ja_JP-hi_fi_captain-medium.onnx"

TEXT="このセリフを喋らせる。"
echo "${TEXT}" | "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus" --model "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/models/ja_JP-hi_fi_captain-medium.onnx" --output_file  "test.wav"


```sh
$ echo "${TEXT}" | "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus" --model "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/models/ja_JP-hi_fi_captain-medium.onnx" --output_file  "test.wav"
[2026-09-22 20:19:08.783] [piper] [warning] Unknown phoneme_type 'japanese', defaulting to MultilingualPhonemes
[2026-09-22 20:19:08.783] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```



これは本家も試したいな。またビルドする羽目になるのか。それとも本家ならビルド済みバイナリがあるのか？

え、もう三年近く前じゃん。それが最新なの？　「2023.11.14-2 Latest」　URLを確認するとそうだった。

https://github.com/rhasspy/piper/releases/

辻褄が合わなくなってきたぞ。

https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang

> ⚠️ Breaking change (2026-05): このモデルは MB-iSTFT-VITS2 アーキテクチャの新世代版です。piper-plus PR #320 以降のランタイムで動作します。出力形状 [B, 1, T] は維持されているため、C++/Rust/C#/Go/WASM ランタイムは修正不要で本モデルを推論できます。

やっぱcss10は本家じゃなくPiperPlus用モデルなのでは？　だって本家は2023年で開発が止まってるんでしょ？なら2026-05の新世代版なんて存在しないよね？

でもだったらなぜpiper-plusで使えないの？

訳わからん。




いやだから0.7.0は先月だから大丈夫って言ったろうが。何回同じこと言わせんだよバカ。

> したがって、「2026年5月のモデル」は本家 Piper（2023年版）では動かず、最新の PiperPlus（main ブランチ）専用のモデルだったのです。

でしょうね。だって以下URLにも`plus`って書いてるし。

https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang

でも最新安定版0.7.0で使えないはずないんだが。





じゃあ以下の音声モデルが動かない理由は？

https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/samples/speaker_0.mp3
https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/samples/speaker_1.mp3

MODEL_CARDの中にもSpeakers: 2とある。

* Language: ja_JP (Japanese, Japan)
* Speakers: 2
* Quality: medium
* Samplerate: 22,050Hz

```
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx.json
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/MODEL_CARD
wget https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ALIASES
```

```sh
$ echo "${TEXT}" | "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/piper-plus/build/piper-plus" --model "/home/pi/root/sys/env/tool/piper-plus/npm-v0.7.0/models/ja_JP-hi_fi_captain-medium.onnx" --output_file  "test.wav"
[2026-09-22 20:19:08.783] [piper] [warning] Unknown phoneme_type 'japanese', defaulting to MultilingualPhonemes
[2026-09-22 20:19:08.783] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```


実在する音声モデルは以下3つ発見した。このうちつくよみちゃんだけは動作確認した。そしてcss10は動作しない。Geminiは音声モデルのバグだと主張している。最後はpiperか。これをこれから動作確認しよう。

* piper-plusで動いた: https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/tsukuyomi-chan-6lang-fp16.onnx
* piper-plusで動くはずだがエラー: https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/css10-ja-6lang-fp16.onnx
* piper本家で動くはず: `https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx`






なんか失敗した。

```sh
piper/libonnxruntime.so
[2026-09-22 20:42:30.652] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
/tmp/work/Shell.Piper.Install.20260922202326/src/1/install.sh: 33 行: 26022 終了                  echo "こんにちは。本家 Piper の日本語テストです。"
     26023 中止                  | ./piper --model models/ja_JP-hi_fi_captain-medium.onnx --speaker 0 --output_file test_speaker0.wav
[2026-09-22 20:42:30.797] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
/tmp/work/Shell.Piper.Install.20260922202326/src/1/install.sh: 40 行: 26024 終了                  echo "こんにちは。本家 Piper の日本語テストです。"
     26025 中止                  | ./piper --model models/ja_JP-hi_fi_captain-medium.onnx --speaker 1 --output_file test_speaker1.wav
test_speaker0.wav: そのようなファイルやディレクトリはありません
test_speaker1.wav: そのようなファイルやディレクトリはありません
```

問題のプルリク#320なんだが。

https://github.com/ayutaz/piper-plus/pull/320

これまだ開発中なのか？　だから最新安定版には実装されていない？　発見は2026/05だったし、音声モデルもそれに対応したcss10があるけど、まだ最新安定版じゃないのか？　ステータスはClosedになってないよね？　Mergedになってる。つまりこれ、開発版にマージしてある状態ってことか？　だから最新安定版0.7.0には実装されておらず、音声モデルが読み込めなかった、ということなのでは？

```
ayutaz
merged 22 commits into
dev
from
feat/mb-istft-vits2
on May 3
```



紛らわしくてすまん。今回のスクリプトの本家が失敗したのと、さっきいったプルリクは別件だ。これまでの話を理解できていたら読み取れるはずなんだがね。プルリクはcss10の話。本家の失敗とは別件。

1. 本家 Piper（2023.11.14-2）で hi_fi_captain が落ちた理由




つまり以下ってことか。

* piper-plus(main:npm-0.7.0)で動いた: https://huggingface.co/ayousanz/piper-plus-tsukuyomi-chan/resolve/main/tsukuyomi-chan-6lang-fp16.onnx
* piper-plusで動くはずだがエラー(プルリク#320によると原因はmainでなくdev（開発版）であるべき所main版を使っていたからだと思われる): https://huggingface.co/ayousanz/piper-plus-css10-ja-6lang/resolve/main/css10-ja-6lang-fp16.onnx
* piper本家で動くはず（エラー。現在調査中）: `https://huggingface.co/rhasspy/piper-voices/blob/main/ja/ja_JP/hi_fi_captain/medium/ja_JP-hi_fi_captain-medium.onnx`

とりあえずpiper本家を動くようにしよう。日本語 G2P 前処理って奴？どうやったらできるの？




私としては開発版が必須というのが嫌だ。安定するまでは使わないようにしたい。ボイスが少なくて残念ではあるけど。

```
$ pip install pyopenjtalk
error: externally-managed-environment

× This environment is externally managed
╰─> To install Python packages system-wide, try apt install
    python3-xyz, where xyz is the package you are trying to
    install.
    
    If you wish to install a non-Debian-packaged Python package,
    create a virtual environment using python3 -m venv path/to/venv.
    Then use path/to/venv/bin/python and path/to/venv/bin/pip. Make
    sure you have python3-full installed.
    
    For more information visit http://rptl.io/venv

note: If you believe this is a mistake, please contact your Python installation or OS distribution provider. You can override this, at the risk of breaking your Python installation or OS, by passing --break-system-packages.
hint: See PEP 668 for the detailed specification.
```

これ結局仮想環境をディスクに作らなきゃいけないのでは……。動くかどうかもまだ分からないのに？　嫌だなぁ。RAMディスクで動作確認してからにしたい。





ああ、公式がOpenJTalkを必要とするクソ仕様だからpiper-plusが作られたってことか。

ところで本家piperを展開したけど、これはパスを変更しても動作するんだろうか？

例によって失敗した。間に`2023.11.14-2`というバージョン用ディレクトリを差し込んだだけなんだけどなぁ。

```sh
$ /tmp/work/piper_official/piper/2023.11.14-2/piper
terminate called after throwing an instance of 'std::runtime_error'
  what():  Model file doesn't exist
中止
```

これも前みたいに解決できるのかな？　それともまずはlddだっけ？　それで調査すべきか？

スクリプトそっちのけで、なんでそんなことしてたかって？　めちゃくちゃ長いからだよ。PyOpenJTalkが長すぎるって。venvでも長かったのにtestも長い。辞書のダウンロードだけで長い。

```
$ /tmp/work/Shell.Piper.Install.20260922211536/src/2/test.py
Downloading: "https://github.com/r9y9/open_jtalk/releases/download/v1.11.1/open_jtalk_dic_utf_8-1.11.tar.gz"
100%|████████████████████████████████████████████████████████████████████████████████████| 22.6M/22.6M [03:19<00:00, 119kB/s]
Extracting tar file
G2P変換結果: k o N n i ch i w a pau h o N k e p a i p a a t o h a i f a i ky a p U t e N n o t e s U t o d e s U
Traceback (most recent call last):
  File "/tmp/work/Shell.Piper.Install.20260922211536/src/2/test.py", line 25, in <module>
    p0 = subprocess.Popen(cmd_speaker0, stdin=subprocess.PIPE, text=True)
         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.11/subprocess.py", line 1024, in __init__
    self._execute_child(args, executable, preexec_fn, close_fds,
  File "/usr/lib/python3.11/subprocess.py", line 1901, in _execute_child
    raise child_exception_type(errno_num, err_msg, err_filename)
FileNotFoundError: [Errno 2] No such file or directory: '/tmp/work/piper_official/piper/piper'
```

ああ、パスにバージョンディレクトリ挟んだからね。元に戻そう。

```
$ /tmp/work/Shell.Piper.Install.20260922211536/src/2/test.py
G2P変換結果: k o N n i ch i w a pau h o N k e p a i p a a t o h a i f a i ky a p U t e N n o t e s U t o d e s U
[2026-09-22 21:24:25.084] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
[2026-09-22 21:24:25.197] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
生成完了: output_speaker0.wav / output_speaker1.wav
```

ダメだった。しかもこのエラーって例の奴じゃん。本家piperでも解決しないのかよ。どゆこと？








> 1. 本家 Piper: 日本語をそのまま扱えないため、外部の G2P（OpenJTalk）が必須。しかし、OpenJTalk が吐く音素とモデル側の音素マップの形式を完璧に一致させる変換層を書かないと、aɪ で落ちる。

これはもう解決できないっしょ。ていうかそれを解決したのがpiper-plusと、つくよみちゃん、ということなんだろうな。

え、じゃあ他の２つの音声モデルは、一体どうやって日本語を発話するんだよ。絶対なんか方法あるはずでしょ。特に片方はmp3ファイルで音声例を確認した。動作する環境があるから音声ファイルが作成できたはず。だから絶対に環境構築は可能なはずなんだが。

もしかしてつくよみちゃん以外の２つの音声モデルは、piper-plusの開発版でしか動作しないのか？　バカな。最も安定しているはずの本家piperで動作しないとか、ありえるか？

本家piperで動作する日本語の音声モデルは？　一つもないってこと？





嘘乙。

```sh
(test_venv) pi@raspberrypi:/tmp/work $ # 本家 Piper の正しい実行方法（OpenJTalk 不要）
echo "こんにちは。パイパーのテストです。" | \
  /tmp/work/piper_official/piper/piper \
  --model /tmp/work/piper_official/piper/models/ja_JP-hi_fi_captain-medium.onnx \
  --speaker 0 \
  --output_file test.wav
[2026-09-22 21:30:45.302] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```
```
pi@raspberrypi:/tmp/work $ # 1. 本家 piper のディレクトリに移動 (これが重要です)
pi@raspberrypi:/tmp/work $ cd /tmp/work/piper_official/piper
pi@raspberrypi:/tmp/work/piper_official/piper $ 
pi@raspberrypi:/tmp/work/piper_official/piper $ # 2. 男声 (speaker 0)
pi@raspberrypi:/tmp/work/piper_official/piper $ echo "こんにちは、はじめまして。" | ./piper \
>   --model models/ja_JP-hi_fi_captain-medium.onnx \
>   --speaker 0 \
>   --output_file test_m.wav
[2026-09-22 21:31:41.247] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```

本家piperでは未だに発話確認ができない。下手な日本語が聞けるはずなんだが。piper-plusが開発される原因となった下手な日本語を聞きたいんだが。



> サンプル MP3 の作成者は、本家 piper バイナリ単体ではなく、piper-phonemize（専用の音素化 C++ ライブラリ）と OpenJTalk（C++版）を組み込んで独自ビルドした環境、または Python 版の piper-tts パッケージ を通して音素（Phonemes）を厳密な ID に変換した上で推論を行っています。

情報源どこ？　また適当なこと言ってるだろ。

いいからpiperでの発話を確認させてくれよ。どうしたら発話できるの？



ダメなんだが。

```sh
$ cd /tmp/work/piper_official/piper

echo "k o N n i ch i w a" | ./piper \
  --model models/ja_JP-hi_fi_captain-medium.onnx \
  --speaker 0 \
  --output_file test_m.wav
[2026-09-22 21:34:50.552] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```



じゃあ本家piperで動作する音声モデルはどれなのさ。今使ってるモデルは既にpiper-plusでも動作しなかった奴でしょ？　どこでも使えない音声モデルとかある？　ありえないっしょ。何がどうなってるんだ？さっぱりわからんぞ。



> hi_fi_captain や kanagawa などの日本語モデルは、espeak-ng ではなく OpenJTalk（日本語専用G2P） を使って音素変換するように作られています（JSON 内の "phoneme_type": "japanese" がその証拠です）。

その情報源はどこ？　URL出せ。

> このモデルは CLI バイナリから直接使うものではなく、Python パッケージ（pip install piper-tts）経由 で動かす前提で作られています。Python 版の piper-tts パッケージには OpenJTalk のバインディングが含まれているため、Python 経由であればこのモデルを読み込んで発話させることができます。

はい？　なにそれ。じゃあなんで最初からそれにしなかったの？　「本家は必ず`piper-tts`経由でしか動かせない」ってことになるんだが。どうせお前のハルシネーションだろ？　だってそれじゃ本家のバイナリは何もできないゴミになるじゃんか。




謝罪なんて聞いてないんだよ。本家piperで発話確認したいの。

```
$ cd /tmp/work/piper_official/piper

echo "k o n n i ch i w a" | ./piper \
  --model models/ja_JP-hi_fi_captain-medium.onnx \
  --speaker 0 \
  --output_file test_m.wav
[2026-09-22 21:41:23.311] [piper] [error] "aɪ" is not a single codepoint (ids=161,)
terminate called after throwing an instance of 'std::runtime_error'
  what():  Phonemes must be one codepoint (phoneme id map)
中止
```

何回同じこと言うのさ。もう英語でも何でもいいから。piperで発話できないの？そんなわけないよね？




発話を確認した。
つまり日本語の発話ができないだけで、英語なら可能ってことか。
え、じゃあつくよみちゃん以外の日本語音声モデルはなんなの？
piper-plusの開発版でのみ再生できるってことか？　どう思う？




つまりpiperと一口に言っても、以下4つの実行バイナリと音声モデルの組み合わせがあるってこと？　4*4=16通り。これが噛み合わないと発話できないってことか。で、今私は本家piperで英語と、piper-pulsによる`tsukuyomi-chan`を確認したと。ええ、ほぼ同じバイナリを4つ用意しなきゃいけないとかマジかよ……。で？結局hi_fi_captainはどうやって発話させるの？

* 本家 C++ 単体バイナリ (piper)
	* 英語など espeak-ng 対応言語専用。単体バイナリでの日本語発話は不可。
* piper-plus 安定版 (v0.7.0 / main)
	* tsukuyomi-chan（旧仕様フォーマット）のみ安定動作。
* piper-plus 開発版 (dev)
	* css10-ja-6lang などの最新モデルが動作するが、未リリースの開発ブランチ必須。
* 本家 Python パッケージ（または OpenJTalk 拡張を組み込んでビルドした本家環境）依存のモデル
	* hi_fi_captain や kanagawa （本家 Piper の日本語モデル）

`piper hi_fi_captain`で検索すると以下記事がヒット。なんかこの

https://note.com/tane_log/n/n3adb8632824f

> Piperが2026年9月14日に日本語対応――Home Assistantで完全ローカル音声アシスタントを作る 

このタイトルはおかしい。piperの更新は2023年で停止している。ということはpiper-plusか？

> 一方、音声合成のPiperについては状況が変わりました。Home Assistant公式アドオンのリポジトリで、2026年9月10日に作成され9月14日にマージされたプルリクエストにより、Piperアドオンにenable_japaneseという設定オプションが追加されています。これは本稿公開時点のわずか1週間前の変更です。実際、2026年7月時点の解説記事では、Piperのコアエンジンであるpiper1-gplについて「公式な日本語ボイスモデルはまだ存在しない」という趣旨の記載があり、この機能がいかに最近追加されたものかがわかります。

どういうこと？　コアエンジンの更新は続いているってこと？

https://github.com/OHF-voice/piper1-gpl

releaseは3週間前。更新されてるな。2026/09/14より前だけど。

READMEをみると以下があった。

```
pip install piper-tts
```

え、これお前がさっき言ってた奴じゃん。もしやこれなら使えるのか？






試してみるしか無いのか。つまりビルドやり直し。
うん、これは明日にしよう。



piper-plus
